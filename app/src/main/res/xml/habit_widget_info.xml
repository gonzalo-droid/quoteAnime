<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/habit_widget_description"
    android:configure="com.gondroid.quoteanime.presentation.widget.HabitWidgetConfigureActivity"
    android:minWidth="110dp"
    android:minHeight="80dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:maxResizeWidth="400dp"
    android:maxResizeHeight="250dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/habit_widget_preview" />
<!--
    android:configure launches HabitWidgetConfigureActivity right after the widget is
    dropped on the home screen, so the user picks which habit THIS instance tracks —
    every other value here mirrors quote_widget_info.xml's conventions.
-->
