<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="Theme.QuoteAnime" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.QuoteAnime.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_dark</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_app_logo_background</item>
        <item name="postSplashScreenTheme">@style/Theme.QuoteAnime</item>
        <item name="android:windowBackground">@color/bg_dark</item>
    </style>

</resources>
